#include<stdio.h>
int main(){
    int length,breadth;
    printf("enter the length=");
    scanf("%d",&length);
    printf("enter the breadth=");
    scanf("%d",&breadth);
    printf("the area of the rectangle=%d",length*breadth);
    return 0;
}